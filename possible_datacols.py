# Possible data columns

data_cols = ['colums',
             'redacted',
             'for',
             'data',
             'protection',
             'purposes'
]

data_cols = [x.upper() for x in data_cols]
